/* eslint-disable no-unused-expressions */

import { expect } from 'chai'

describe('encode', () => {
  it('encodes unit properly', () => {
    const result = true
    expect(result).to.be.true
  })
})
